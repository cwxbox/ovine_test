/**
 * TODO: 将主题相关的东西，要么写到scss中，要么定义到styled中
 * 由于主题变量太多，没有全都注入到styled中，因此先偷懒，分别对每个主题单独写
 */
export declare const StyledRouteTabs: import("styled-components").StyledComponent<"div", import("styled-components").DefaultTheme, {}, never>;
