/**
 * ovine cli main webpack config file.
 */
import { Configuration } from 'webpack';
import { BuildCliOptions, DevCliOptions, Props } from '../types';
declare type BaseConfigOptions = Props & Partial<DevCliOptions> & Partial<BuildCliOptions>;
export declare function createBaseConfig(options: BaseConfigOptions): Promise<Configuration>;
declare type ManifestInfo = {
    hostDir: string;
    assetsFile: string;
    manifestFile: string;
    assetJson: any;
};
export declare function loadDllManifest(options: Props & Partial<BuildCliOptions>): Promise<ManifestInfo>;
export {};
