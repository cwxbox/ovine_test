/**
 * load & check site config
 */
import { BuildCliOptions, LoadContext, SiteConfig } from './types';
export declare function loadConfig(siteDir: string, options: Partial<BuildCliOptions>): SiteConfig;
export declare function loadContext(siteDir: string, options: Partial<BuildCliOptions>): LoadContext;
